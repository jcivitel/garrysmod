version: 2.1
workflows:
  build:
    jobs:
      - build:
          context:
            - dockerhub
jobs:
  build:
    working_directory: ~/app
    docker:
      - image: cimg/base:current
        auth:
          username: $DOCKERHUB_USERNAME
          password: $DOCKERHUB_PASSWORD
    steps:
      - checkout
      - run:
          name: Init git submodule
          command: |
            git submodule update --init --recursive
      - setup_remote_docker:
          docker_layer_caching: true
      - run:
          name: build process
          command: |      
            docker build -t jcivitell/$CIRCLE_PROJECT_REPONAME .
      - run:
          name: upload image
          command: |
            chmod +x build-helper-scripts/semantic-versioning-now.sh
            IMAGEVERSION=$(build-helper-scripts/semantic-versioning-now.sh)
            echo $DOCKERHUB_PASSWORD | docker login -u $DOCKERHUB_USERNAME --password-stdin
            docker image tag jcivitell/$CIRCLE_PROJECT_REPONAME jcivitell/$CIRCLE_PROJECT_REPONAME:latest
            docker image tag jcivitell/$CIRCLE_PROJECT_REPONAME jcivitell/$CIRCLE_PROJECT_REPONAME:$IMAGEVERSION
            docker push -a jcivitell/$CIRCLE_PROJECT_REPONAME
